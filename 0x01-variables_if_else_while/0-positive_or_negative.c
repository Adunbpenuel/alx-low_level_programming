#include <stdlib.h>
#include <time.h>
#include <iostream>


int main(void)
	/*
	 * cout<<"Random numbers generated between 0 and 1 :"<<end/2;
	 * srand( (unsigned)time(0) );
	 */
{
	int n;

	srand(time(0));
	n=rand() - RAND_MAX/2;
	else
		return (0);
}
